{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile on the CampusPulse app.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user profile."
        },
        "email": {
          "type": "string",
          "description": "User's email address. Must be a @gla.ac.in or @gmail.com domain.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "User's first name."
        },
        "lastName": {
          "type": "string",
          "description": "User's last name."
        },
        "karmaPoints": {
          "type": "number",
          "description": "User's accumulated karma points."
        },
        "transactionCount": {
          "type": "number",
          "description": "Number of transactions the user has made today."
        },
        "averageRating": {
          "type": "number",
          "description": "The user's average feedback rating from 1 to 5."
        },
        "ratingsCount": {
          "type": "number",
          "description": "The total number of ratings the user has received."
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName",
        "karmaPoints",
        "transactionCount",
        "averageRating",
        "ratingsCount"
      ]
    },
    "ItemListing": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ItemListing",
      "type": "object",
      "description": "Represents an item listed for rent by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the item listing."
        },
        "ownerId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N ItemListing)"
        },
        "name": {
          "type": "string",
          "description": "Name of the item."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the item's image.",
          "format": "uri"
        },
        "karma": {
          "type": "number",
          "description": "Karma points awarded for renting the item."
        },
        "available": {
          "type": "boolean",
          "description": "Indicates if the item is currently available for rent."
        }
      },
      "required": [
        "id",
        "ownerId",
        "name",
        "imageUrl",
        "karma",
        "available"
      ]
    },
    "Transaction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transaction",
      "type": "object",
      "description": "Represents a rental transaction between two users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique ID for the transaction."
        },
        "fulfillerId": {
          "type": "string",
          "description": "The UID of the user lending the item."
        },
        "requesterId": {
          "type": "string",
          "description": "The UID of the user borrowing the item."
        },
        "itemId": {
          "type": "string",
          "description": "Reference to the original ItemRequest ID."
        },
        "itemName": {
          "type": "string",
          "description": "Name of the item being transacted."
        },
        "itemImageUrl": {
          "type": "string",
          "description": "Image URL of the item."
        },
        "karma": {
          "type": "number",
          "description": "Karma points for the transaction."
        },
        "status": {
          "type": "string",
          "description": "The current state of the transaction.",
          "enum": [
            "CREATED",
            "HANDOVER_PENDING",
            "ACTIVE",
            "RETURN_PENDING",
            "COMPLETED",
            "CANCELLED"
          ]
        },
        "handoverCode": {
          "type": [
            "string",
            "null"
          ],
          "description": "The 4-digit code for the initial handover, stored in plaintext for display."
        },
        "handoverCodeHash": {
          "type": [
            "string",
            "null"
          ],
          "description": "The hashed 4-digit code for the initial handover."
        },
        "handoverVerified": {
          "type": "boolean",
          "description": "Whether the handover code has been successfully verified."
        },
        "returnCode": {
          "type": [
            "string",
            "null"
          ],
          "description": "The 4-digit code for the return, stored in plaintext for display."
        },
        "returnCodeHash": {
          "type": [
            "string",
            "null"
          ],
          "description": "The hashed 4-digit code for the return."
        },
        "returnVerified": {
          "type": "boolean",
          "description": "Whether the return code has been successfully verified."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the transaction was initiated."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the transaction was last updated."
        },
        "requesterLocation": {
          "type": [
            "object",
            "null"
          ],
          "description": "The geographical coordinates where the item was requested (borrower's location).",
          "properties": {
            "lat": {
              "type": "number"
            },
            "lng": {
              "type": "number"
            }
          }
        },
        "fulfillerLocation": {
          "type": [
            "object",
            "null"
          ],
          "description": "The geographical coordinates of the user lending the item (lender's location).",
          "properties": {
            "lat": {
              "type": "number"
            },
            "lng": {
              "type": "number"
            }
          }
        }
      },
      "required": [
        "id",
        "fulfillerId",
        "requesterId",
        "itemId",
        "itemName",
        "status",
        "createdAt",
        "updatedAt"
      ]
    },
    "ItemRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ItemRequest",
      "type": "object",
      "description": "Represents a user's request for an item.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the item request."
        },
        "requesterId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N ItemRequest)"
        },
        "itemName": {
          "type": "string",
          "description": "Name of the requested item."
        },
        "reason": {
          "type": "string",
          "description": "Reason for the item request."
        },
        "urgency": {
          "type": "string",
          "description": "Urgency level of the request (Emergency, Medium, Normal)."
        },
        "requiredBy": {
          "type": "string",
          "description": "The date and time the item is expected to be available.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The status of the request.",
          "enum": [
            "PENDING",
            "FULFILLED"
          ]
        },
        "location": {
          "type": "object",
          "description": "The geographical coordinates where the item is requested.",
          "properties": {
            "lat": {
              "type": "number"
            },
            "lng": {
              "type": "number"
            }
          }
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the request was created."
        }
      },
      "required": [
        "id",
        "requesterId",
        "itemName",
        "reason",
        "urgency",
        "requiredBy",
        "status",
        "createdAt"
      ]
    },
    "Feedback": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Feedback",
      "type": "object",
      "description": "Represents feedback given by one user to another after a transaction.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the feedback."
        },
        "transactionId": {
          "type": "string",
          "description": "The ID of the completed transaction."
        },
        "raterId": {
          "type": "string",
          "description": "The UID of the user giving the feedback."
        },
        "ratedUserId": {
          "type": "string",
          "description": "The UID of the user being rated."
        },
        "rating": {
          "type": "number",
          "description": "The rating given, from 1 to 5."
        },
        "comment": {
          "type": "string",
          "description": "An optional comment."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the feedback was created."
        },
        "raterFirstName": {
          "type": "string",
          "description": "First name of the user who gave the feedback."
        },
        "raterLastName": {
          "type": "string",
          "description": "Last name of the user who gave the feedback."
        },
        "raterPhotoURL": {
          "type": "string",
          "format": "uri",
          "description": "The photo URL of the person who gave the rating."
        }
      },
      "required": [
        "id",
        "transactionId",
        "raterId",
        "ratedUserId",
        "rating",
        "createdAt"
      ]
    },
    "Message": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Message",
      "type": "object",
      "description": "Represents a single chat message within a transaction.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique ID for the message."
        },
        "senderId": {
          "type": "string",
          "description": "The UID of the user sending the message."
        },
        "text": {
          "type": "string",
          "description": "The content of the message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the message was sent."
        }
      },
      "required": [
        "id",
        "senderId",
        "text",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/userProfiles/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information. Uses path-based ownership.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/itemListings",
        "definition": {
          "entityName": "ItemListing",
          "schema": {
            "$ref": "#/backend/entities/ItemListing"
          },
          "description": "Stores item listings. Includes denormalized 'ownerId' for authorization independence.",
          "params": []
        }
      },
      {
        "path": "/transactions",
        "definition": {
          "entityName": "Transaction",
          "schema": {
            "$ref": "#/backend/entities/Transaction"
          },
          "description": "Stores transaction details.",
          "params": []
        }
      },
      {
        "path": "/itemRequests",
        "definition": {
          "entityName": "ItemRequest",
          "schema": {
            "$ref": "#/backend/entities/ItemRequest"
          },
          "description": "Stores item requests.",
          "params": []
        }
      },
       {
        "path": "/feedback/{feedbackId}",
        "definition": {
          "entityName": "Feedback",
          "schema": {
            "$ref": "#/backend/entities/Feedback"
          },
          "description": "Stores feedback for completed transactions.",
          "params": [
            {
              "name": "feedbackId",
              "description": "The unique identifier for the feedback."
            }
          ]
        }
      },
      {
        "path": "/transactions/{transactionId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": {
            "$ref": "#/backend/entities/Message"
          },
          "description": "Stores chat messages for a specific transaction.",
          "params": [
            {
              "name": "transactionId",
              "description": "The unique identifier for the transaction."
            },
            {
              "name": "messageId",
              "description": "The unique identifier for the message."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to ensure authorization independence, clarity, and scalability. User profiles and item listings are stored in separate collections to optimize querying. Item requests are handled in a separate collection as well. Transactions are stored in a dedicated collection. To ensure authorization independence, the `ownerId` is stored within each `ItemListing` document, allowing rules to validate ownership without complex `get()` calls. Similarly, `requesterId` is stored in `ItemRequest`. Each `Transaction` stores `fulfillerId`, `requesterId`, and `itemId` for similar reasons.\n\nThe structure also facilitates secure list operations. By segregating data into collections based on entity type (users, items, requests, transactions), rules can be applied to entire collections rather than relying on document-level filtering. Using the dedicated collections approach alongside path-based ownership for private data and existence-over-content for global roles allows us to achieve Authorization Independence, DBAC, and support QAPs, thereby supporting secure list operations."
  }
}
